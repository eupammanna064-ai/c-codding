// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
    int a=4,b=5,temp;
    temp=a;
    a=b;
    b=temp;
    printf("%d,%d,a,b");

    return 0;
}
